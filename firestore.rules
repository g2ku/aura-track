rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Публичные данные — только чтение для всех
    match /public/{document} {
      allow read: if true;
      allow write: if false;
    }

    // Данные пользователей
    match /users/{uid} {
      allow read: if request.auth != null;
      // Can create own profile; can update only own profile (except admins via Admin SDK)
      allow create: if request.auth != null && request.auth.uid == uid;
      allow update: if request.auth != null && request.auth.uid == uid;
      // Delete only via Admin SDK
      allow delete: if false;
    }

    // Настройки — чтение для авторизованных, запись для авторизованных
    match /settings/{docId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }

    // Рабочие данные — только серверный доступ (через Admin SDK)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
